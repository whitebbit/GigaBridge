# Система управления инструкциями

## Обзор

Система управления инструкциями позволяет администраторам создавать и управлять инструкциями для разных платформ с поддержкой видео, текста и файлов. Инструкции динамически загружаются из базы данных и отправляются пользователям при выборе платформы.

## Структура базы данных

### Таблица `platforms` (Платформы)

Хранит информацию о платформах (PC, Mobile и т.д.):

- `id` - Уникальный идентификатор
- `name` - Техническое имя платформы (латиница, без пробелов, например: `pc`, `mobile`)
- `display_name` - Отображаемое название (например: "💻 ПК", "📱 Телефоны")
- `description` - Описание платформы (опционально)
- `is_active` - Активна ли платформа
- `order` - Порядок отображения
- `created_at` - Дата создания
- `updated_at` - Дата обновления

### Таблица `tutorials` (Туториалы/Инструкции)

Хранит инструкции для каждой платформы:

- `id` - Уникальный идентификатор
- `platform_id` - Связь с платформой
- `title` - Заголовок туториала
- `text` - Текст инструкции (поддерживается HTML)
- `video_file_id` - file_id видео в Telegram (опционально)
- `video_note_id` - file_id видеосообщения (круглое видео) в Telegram (опционально)
- `is_basic` - Базовый туториал (True) или дополнительный (False)
- `order` - Порядок отображения
- `is_active` - Активен ли туториал
- `created_at` - Дата создания
- `updated_at` - Дата обновления

### Таблица `tutorial_files` (Файлы туториалов)

Хранит файлы, прикрепленные к туториалам:

- `id` - Уникальный идентификатор
- `tutorial_id` - Связь с туториалом
- `file_id` - file_id файла в Telegram
- `file_name` - Имя файла для отображения
- `file_type` - Тип файла (document, photo, video и т.д.)
- `description` - Описание файла (опционально)
- `order` - Порядок отображения
- `created_at` - Дата создания

## Использование админ-панели

### Доступ к управлению инструкциями

1. Откройте админ-панель (команда `/admin` или кнопка для администраторов)
2. Выберите "📖 Инструкции"

### Управление платформами

#### Добавление новой платформы

1. В меню инструкций выберите "🌐 Платформы"
2. Нажмите "➕ Добавить платформу"
3. Введите техническое имя платформы (латиница, без пробелов, например: `pc`, `mobile`, `router`)
4. Введите отображаемое название (например: "💻 ПК", "📱 Телефоны")
5. Введите описание (или отправьте `/skip` для пропуска)

#### Редактирование платформы

1. Выберите платформу из списка
2. Доступные действия:
   - ✏️ Изменить название
   - 📝 Изменить описание
   - 🔄 Переключить статус (активна/неактивна)
   - 📖 Управлять туториалами
   - 🗑️ Удалить платформу

### Управление туториалами

#### Добавление туториала

1. Выберите платформу и нажмите "📖 Управлять туториалами"
2. Нажмите "➕ Добавить туториал"
3. Введите заголовок туториала
4. Введите текст инструкции (HTML поддерживается, или отправьте `/skip`)
5. Выберите тип туториала:
   - 📘 Базовый - основная инструкция, показывается первой
   - 📗 Дополнительный - дополнительная информация, показывается по кнопке "Узнать больше"

#### Загрузка видео для туториала

1. Выберите туториал для редактирования
2. Нажмите "🎥 Загрузить видео"
3. Отправьте видео файл или видеосообщение (круглое видео)
4. Видео будет автоматически прикреплено к туториалу

**Примечание:** 
- Можно загрузить либо обычное видео (`video_file_id`), либо видеосообщение (`video_note_id`)
- При отправке пользователю видео отправляется вместе с текстом инструкции

#### Добавление файлов к туториалу

1. Выберите туториал для редактирования
2. Нажмите "📎 Управлять файлами"
3. Нажмите "➕ Добавить файл"
4. Отправьте файл (документ, архив, установщик и т.д.)
5. Файл будет автоматически прикреплен к туториалу

**Поддерживаемые типы файлов:**
- Документы (PDF, DOC, DOCX и т.д.)
- Архивы (ZIP, RAR и т.д.)
- Установщики (EXE, DMG, APK и т.д.)
- Любые другие файлы, которые можно отправить через Telegram

#### Редактирование туториала

1. Выберите туториал из списка
2. Доступные действия:
   - ✏️ Изменить заголовок
   - 📝 Изменить текст
   - 🎥 Загрузить видео
   - 📎 Управлять файлами
   - 🔄 Переключить тип (базовый/дополнительный)
   - 🔄 Переключить статус (активен/неактивен)
   - 🗑️ Удалить туториал

## Как это работает для пользователей

### Просмотр инструкций

1. Пользователь нажимает кнопку "📖 Инструкции" в главном меню
2. Выбирает платформу из списка
3. Получает базовый туториал с:
   - Текстом инструкции
   - Видео (если загружено)
   - Файлами (если добавлены)
4. Может нажать "📚 Узнать больше" для просмотра дополнительных туториалов

### Порядок отправки контента

При отправке туториала пользователю:

1. **Видео** (если есть) отправляется первым с текстом инструкции в подписи
2. **Текст** отправляется отдельно, если видео нет или это видеосообщение
3. **Файлы** отправляются по очереди после видео/текста

## API функции

### Работа с платформами

```python
from utils.db import (
    get_all_platforms,
    get_active_platforms,
    get_platform_by_id,
    get_platform_by_name,
    create_platform,
    update_platform,
    delete_platform
)

# Получить все платформы
platforms = await get_all_platforms()

# Получить активные платформы
active_platforms = await get_active_platforms()

# Получить платформу по ID
platform = await get_platform_by_id(platform_id)

# Создать платформу
platform = await create_platform(
    name="pc",
    display_name="💻 ПК",
    description="Инструкции для компьютеров",
    is_active=True,
    order=0
)
```

### Работа с туториалами

```python
from utils.db import (
    get_tutorials_by_platform,
    get_tutorial_by_id,
    get_basic_tutorial_for_platform,
    get_additional_tutorials_for_platform,
    create_tutorial,
    update_tutorial,
    delete_tutorial
)

# Получить базовый туториал для платформы
tutorial = await get_basic_tutorial_for_platform(platform_id)

# Получить дополнительные туториалы
additional = await get_additional_tutorials_for_platform(platform_id)

# Создать туториал
tutorial = await create_tutorial(
    platform_id=1,
    title="Инструкция для Windows",
    text="<b>Шаг 1:</b> Скачайте приложение...",
    video_file_id="BAACAgIAAxkBAAIB...",
    is_basic=True
)
```

### Работа с файлами

```python
from utils.db import (
    get_tutorial_files,
    add_tutorial_file,
    delete_tutorial_file
)

# Получить файлы туториала
files = await get_tutorial_files(tutorial_id)

# Добавить файл
file = await add_tutorial_file(
    tutorial_id=1,
    file_id="BAACAgIAAxkBAAIB...",
    file_name="installer.exe",
    file_type="document",
    description="Установщик приложения"
)
```

## Миграция базы данных

Для применения изменений в базе данных выполните:

```bash
# Применить миграции
python scripts/migrate.py upgrade head
```

Или через Docker:

```bash
docker exec -it gigabridge_bot python scripts/migrate.py upgrade head
```

## Примеры использования

### Создание полной инструкции для новой платформы

1. **Создайте платформу:**
   - Техническое имя: `router`
   - Отображаемое название: "🔌 Роутеры"
   - Описание: "Инструкции для настройки на роутерах"

2. **Создайте базовый туториал:**
   - Заголовок: "Настройка на роутере"
   - Текст: "Шаг 1: Войдите в панель управления..."
   - Загрузите видео с демонстрацией
   - Добавьте файлы: конфигурационный файл, документацию

3. **Создайте дополнительный туториал:**
   - Заголовок: "Продвинутые настройки"
   - Текст: "Для опытных пользователей..."
   - Добавьте дополнительные файлы

### Обновление существующей инструкции

1. Выберите платформу
2. Выберите туториал
3. Нажмите "📝 Изменить текст" или "🎥 Загрузить видео"
4. Внесите изменения
5. Изменения сразу отобразятся для пользователей

## Рекомендации

1. **Видео:**
   - Используйте короткие видео (до 1 минуты) для лучшей загрузки
   - Видеосообщения (круглые) лучше подходят для быстрых демонстраций
   - Обычные видео лучше для подробных инструкций

2. **Текст:**
   - Используйте HTML для форматирования (жирный, курсив, списки)
   - Разбивайте длинные инструкции на шаги
   - Используйте эмодзи для лучшей читаемости

3. **Файлы:**
   - Называйте файлы понятно (например: "V2rayN_Installer.exe")
   - Добавляйте описания к файлам
   - Группируйте связанные файлы в одном туториале

4. **Организация:**
   - Один базовый туториал на платформу (основная инструкция)
   - Несколько дополнительных туториалов для разных сценариев
   - Используйте порядок (`order`) для сортировки

## Устранение неполадок

### Видео не отправляется пользователям

- Проверьте, что `video_file_id` или `video_note_id` сохранены в базе данных
- Убедитесь, что файл еще доступен в Telegram (file_id может устареть)
- Попробуйте перезагрузить видео

### Файлы не отправляются

- Проверьте, что `file_id` сохранен в базе данных
- Убедитесь, что файл еще доступен в Telegram
- Проверьте права доступа к файлу

### Инструкции не отображаются

- Проверьте, что платформа активна (`is_active = True`)
- Проверьте, что туториал активен (`is_active = True`)
- Убедитесь, что есть базовый туториал для платформы

## Дополнительная информация

- Все изменения применяются сразу для пользователей
- Старые статические инструкции в `utils/texts/messages.py` больше не используются
- Система полностью динамическая и управляется через админ-панель

