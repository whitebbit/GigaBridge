# 🚀 Полная инструкция по запуску бота на Linux сервере

## 📋 Содержание
1. [Требования](#требования)
2. [Установка Docker и Docker Compose](#установка-docker-и-docker-compose)
3. [Клонирование репозитория](#клонирование-репозитория)
4. [Настройка окружения](#настройка-окружения)
5. [Запуск бота](#запуск-бота)
6. [Проверка работоспособности](#проверка-работоспособности)
7. [Решение проблем](#решение-проблем)
8. [Управление ботом](#управление-ботом)

---

## 📦 Требования

- Linux сервер (Ubuntu 20.04+ / Debian 11+ / CentOS 8+)
- Минимум 2 GB RAM
- Минимум 10 GB свободного места на диске
- Доступ к интернету
- Права root или sudo

---

## 🐳 Установка Docker и Docker Compose

### Для Ubuntu/Debian:

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем необходимые пакеты
sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# Добавляем официальный GPG ключ Docker
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Добавляем репозиторий Docker
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Устанавливаем Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Проверяем установку
docker --version
docker compose version

# Добавляем текущего пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER

# Применяем изменения (или перелогиниваемся)
newgrp docker
```

### Для CentOS/RHEL:

```bash
# Устанавливаем необходимые пакеты
sudo yum install -y yum-utils

# Добавляем репозиторий Docker
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# Устанавливаем Docker
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Запускаем Docker
sudo systemctl start docker
sudo systemctl enable docker

# Добавляем пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Проверяем установку
docker --version
docker compose version
```

---

## 📥 Клонирование репозитория

```bash
# Переходим в домашнюю директорию (или туда, где хотите разместить проект)
cd ~

# Клонируем репозиторий
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> GigaBridge
# Или если репозиторий уже есть, просто переходим в директорию
cd GigaBridge

# Проверяем, что мы в правильной директории
ls -la
# Должны увидеть файлы: docker-compose.yml, Dockerfile, main.py и т.д.
```

---

## ⚙️ Настройка окружения

### 1. Создаем файл `.env`

```bash
# Создаем файл .env в корне проекта
nano .env
```

### 2. Заполняем файл `.env` следующими переменными:

```env
# ============================================
# ТОКЕН БОТА (ОБЯЗАТЕЛЬНО)
# ============================================
BOT_TOKEN=ваш_токен_бота_от_BotFather

# ============================================
# НАСТРОЙКИ БАЗЫ ДАННЫХ
# ============================================
# Эти настройки должны совпадать с docker-compose.yml
DB_HOST=db
DB_PORT=5432
DB_NAME=gigabridge_db
DB_USER=postgres
DB_PASSWORD=210203

# ============================================
# НАСТРОЙКИ 3X-UI API
# ============================================
# URL вашего 3X-UI сервера (например: http://192.168.1.100:2053)
HIDDIFY_API_URL=http://ваш_сервер:порт
# API ключ от 3X-UI (находится в настройках панели)
HIDDIFY_API_KEY=ваш_api_ключ

# ============================================
# НАСТРОЙКИ YOOKASSA (ОПЦИОНАЛЬНО)
# ============================================
# Если используете YooKassa для приема платежей
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
WEBHOOK_URL=https://ваш_домен/webhook

# ============================================
# НАСТРОЙКИ СКИДОК
# ============================================
# Процент скидки на первую покупку (по умолчанию 20%)
FIRST_PURCHASE_DISCOUNT_PERCENT=20

# ============================================
# ТЕСТОВЫЙ РЕЖИМ
# ============================================
# true - подписки на 1 минуту (для тестирования)
# false - подписки на 30 дней (продакшн)
TEST_MODE=false

# ============================================
# НАСТРОЙКИ REDIS
# ============================================
# Обычно не нужно менять, если используете Docker Compose
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ============================================
# ПАРОЛЬ ДЛЯ АДМИНСКИХ КОМАНД
# ============================================
# Пароль для команды выдачи безграничной подписки
GRANT_UNLIMITED_PASSWORD=ваш_безопасный_пароль
```

### 3. Сохраняем файл

В nano: `Ctrl+O` (сохранить), `Enter` (подтвердить), `Ctrl+X` (выйти)

### 4. Устанавливаем правильные права доступа

```bash
# Делаем файл .env доступным только для владельца
chmod 600 .env
```

---

## 🚀 Запуск бота

### 1. Собираем Docker образы

```bash
# Переходим в директорию проекта (если еще не там)
cd ~/GigaBridge

# Собираем образы
docker compose build
```

### 2. Запускаем контейнеры

```bash
# Запускаем все сервисы в фоновом режиме
docker compose up -d
```

### 3. Проверяем статус контейнеров

```bash
# Проверяем, что все контейнеры запущены
docker compose ps

# Должны увидеть что-то вроде:
# NAME              STATUS
# gigabridge_bot    Up
# gigabridge_db     Up (healthy)
# gigabridge_redis  Up (healthy)
```

### 4. Смотрим логи

```bash
# Логи всех сервисов
docker compose logs -f

# Логи только бота
docker compose logs -f bot

# Логи базы данных
docker compose logs -f db

# Логи Redis
docker compose logs -f redis
```

### 5. Инициализируем базу данных

```bash
# Применяем миграции базы данных
docker exec -it gigabridge_bot python scripts/migrate.py upgrade head

# Проверяем текущую версию миграций
docker exec -it gigabridge_bot python scripts/migrate.py current
```

### 6. Создаем администратора (опционально)

```bash
# Создаем администратора бота
docker exec -it gigabridge_bot python scripts/set_admin.py
# Следуйте инструкциям скрипта
```

---

## ✅ Проверка работоспособности

### 1. Проверяем контейнеры

```bash
# Все контейнеры должны быть в статусе "Up"
docker compose ps

# Проверяем здоровье сервисов
docker compose ps --format "table {{.Name}}\t{{.Status}}"
```

### 2. Проверяем логи бота

```bash
# Смотрим последние 50 строк логов
docker compose logs --tail=50 bot

# Ищем ошибки
docker compose logs bot | grep -i error

# Должны увидеть что-то вроде:
# "Бот успешно запущен"
# "Миграции применены успешно"
```

### 3. Проверяем подключение к базе данных

```bash
# Подключаемся к базе данных
docker exec -it gigabridge_db psql -U postgres -d gigabridge_db

# В PostgreSQL консоли проверяем таблицы
\dt

# Должны увидеть таблицы: users, servers, subscriptions, payments и т.д.

# Выходим из PostgreSQL
\q
```

### 4. Проверяем работу бота

```bash
# Отправляем команду /start боту в Telegram
# Бот должен ответить

# Проверяем, что бот получает сообщения
docker compose logs bot | tail -20
```

---

## 🔧 Решение проблем

### Проблема 1: Контейнеры не запускаются

```bash
# Проверяем логи
docker compose logs

# Проверяем, не заняты ли порты
sudo netstat -tulpn | grep -E '5432|6379'

# Если порты заняты, измените их в docker-compose.yml
```

### Проблема 2: Ошибка миграций базы данных

```bash
# Проверяем состояние базы данных
docker exec -it gigabridge_bot python scripts/fix_migration_state.py

# Если база данных в неконсистентном состоянии, пересоздаем её
docker compose down -v
docker compose up -d

# Применяем миграции заново
docker exec -it gigabridge_bot python scripts/migrate.py upgrade head
```

### Проблема 3: Бот не отвечает

```bash
# Проверяем, что токен бота правильный
docker compose logs bot | grep -i token

# Проверяем, что бот запущен
docker compose ps bot

# Перезапускаем бота
docker compose restart bot

# Смотрим логи
docker compose logs -f bot
```

### Проблема 4: Ошибка подключения к базе данных

```bash
# Проверяем, что база данных запущена
docker compose ps db

# Проверяем логи базы данных
docker compose logs db

# Проверяем настройки в .env файле
cat .env | grep DB_

# Перезапускаем базу данных
docker compose restart db
```

### Проблема 5: Ошибка "column already exists" при миграциях

```bash
# Эта проблема уже исправлена в коде
# Но если она возникает, проверьте версию миграций
docker exec -it gigabridge_bot python scripts/migrate.py current

# Если нужно, пересоздайте базу данных
docker compose down -v
docker compose up -d
docker exec -it gigabridge_bot python scripts/migrate.py upgrade head
```

---

## 🎮 Управление ботом

### Остановка бота

```bash
# Остановить все контейнеры (данные сохраняются)
docker compose down

# Остановить и удалить все данные
docker compose down -v
```

### Перезапуск бота

```bash
# Перезапустить все сервисы
docker compose restart

# Перезапустить только бота
docker compose restart bot
```

### Обновление бота

```bash
# Останавливаем контейнеры
docker compose down

# Получаем последние изменения из репозитория
git pull

# Пересобираем образы
docker compose build

# Запускаем заново
docker compose up -d

# Применяем новые миграции (если есть)
docker exec -it gigabridge_bot python scripts/migrate.py upgrade head
```

### Просмотр логов

```bash
# Все логи в реальном времени
docker compose logs -f

# Только логи бота
docker compose logs -f bot

# Последние 100 строк логов бота
docker compose logs --tail=100 bot
```

### Резервное копирование базы данных

```bash
# Создать бэкап
docker exec -it gigabridge_bot python scripts/backup.py

# Восстановить из бэкапа
docker exec -it gigabridge_bot python scripts/restore.py /path/to/backup.sql
```

### Вход в контейнер

```bash
# Войти в контейнер бота
docker exec -it gigabridge_bot bash

# Войти в контейнер базы данных
docker exec -it gigabridge_db bash

# Войти в PostgreSQL консоль
docker exec -it gigabridge_db psql -U postgres -d gigabridge_db
```

---

## 📝 Полезные команды

```bash
# Показать использование ресурсов
docker stats

# Показать все контейнеры (включая остановленные)
docker compose ps -a

# Очистить неиспользуемые образы и контейнеры
docker system prune -a

# Показать объем данных
docker system df

# Проверить конфигурацию docker-compose.yml
docker compose config
```

---

## 🔒 Безопасность

### 1. Измените пароль базы данных

В `docker-compose.yml` измените:
```yaml
POSTGRES_PASSWORD: ваш_надежный_пароль
```

И в `.env` файле:
```env
DB_PASSWORD=ваш_надежный_пароль
```

### 2. Настройте файрвол

```bash
# Разрешаем только необходимые порты
sudo ufw allow 22/tcp    # SSH
# PostgreSQL и Redis не нужно открывать наружу, они работают внутри Docker сети
```

### 3. Регулярно обновляйте систему

```bash
sudo apt update && sudo apt upgrade -y
```

---

## 📚 Дополнительная документация

- [Управление миграциями](MIGRATION_COMMANDS.md)
- [Работа с базой данных](README_DATABASE.md)
- [Настройка платежей](PAYMENT_SETUP.md)
- [Использование Docker](DOCKER_USAGE.md)

---

## 🆘 Получение помощи

Если возникли проблемы:

1. Проверьте логи: `docker compose logs -f bot`
2. Проверьте документацию в папке `docs/`
3. Проверьте, что все переменные в `.env` заполнены правильно
4. Убедитесь, что порты не заняты другими приложениями

---

## ✅ Чеклист первого запуска

- [ ] Docker и Docker Compose установлены
- [ ] Репозиторий склонирован
- [ ] Файл `.env` создан и заполнен
- [ ] Права доступа к `.env` установлены (600)
- [ ] Docker образы собраны (`docker compose build`)
- [ ] Контейнеры запущены (`docker compose up -d`)
- [ ] Все контейнеры в статусе "Up" (`docker compose ps`)
- [ ] Миграции применены (`docker exec -it gigabridge_bot python scripts/migrate.py upgrade head`)
- [ ] Бот отвечает на команду `/start` в Telegram
- [ ] Логи не содержат критических ошибок

---

**Готово! Бот должен работать! 🎉**
