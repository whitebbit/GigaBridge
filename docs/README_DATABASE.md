# 🗄️ Управление базой данных

## ✅ Что исправлено

1. **Убрано автоматическое пересоздание таблиц** - таблицы больше не создаются при каждом запуске
2. **Добавлен Alembic** - профессиональная система миграций
3. **Улучшена структура проекта** - все миграции версионируются
4. **Данные сохраняются** - при перезапуске Docker данные не теряются

## 🚀 Первый запуск

### 1. Запустите Docker контейнеры

```bash
docker-compose up -d
```

### 2. Инициализируйте базу данных

```bash
docker exec -it gigabridge_bot python scripts/init_db.py
```

Это создаст начальную миграцию и применит её.

## 📝 Работа с миграциями

### Создание новой миграции

После изменения моделей в `database/models.py`:

```bash
docker exec -it gigabridge_bot python scripts/migrate.py revision --autogenerate -m "описание изменений"
```

### Применение миграций

```bash
docker exec -it gigabridge_bot python scripts/migrate.py upgrade head
```

### Просмотр текущей версии

```bash
docker exec -it gigabridge_bot python scripts/migrate.py current
```

## 📋 Шпаргалка команд

**Быстрая шпаргалка:** [MIGRATION_COMMANDS.md](MIGRATION_COMMANDS.md)

### Основные команды:

```bash
# Создать миграцию после изменения моделей
docker exec -it gigabridge_bot python scripts/migrate.py revision --autogenerate -m "описание"

# Применить миграции
docker exec -it gigabridge_bot python scripts/migrate.py upgrade head

# Просмотреть текущую версию
docker exec -it gigabridge_bot python scripts/migrate.py current
```

## 📚 Подробная документация

См. [MIGRATIONS_README.md](MIGRATIONS_README.md) для полной документации по миграциям.
См. [MIGRATION_COMMANDS.md](MIGRATION_COMMANDS.md) для шпаргалки всех команд.

## ⚠️ Важно

- **Не используйте** `database/init_db.py` напрямую - используйте `scripts/init_db.py` или `scripts/migrate.py`
- **Всегда проверяйте** автогенерированные миграции перед применением
- **Делайте бэкап** перед применением миграций в production

