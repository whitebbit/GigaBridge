# Админ-панель для управления серверами

## Описание

Система управления серверами через админ-панель в телеграм-боте. Администраторы могут добавлять, редактировать и удалять серверы для продажи VLESS ключей.

## Функциональность

- ✅ Добавление новых серверов
- ✅ Просмотр списка всех серверов
- ✅ Редактирование данных сервера (название, цена, локация, описание, API URL, API ключ, максимальное количество пользователей)
- ✅ Активация/деактивация серверов
- ✅ Удаление серверов

## Добавление администраторов

Перед использованием админ-панели необходимо назначить пользователя администратором.

### По Telegram ID

```bash
docker exec -it gigabridge_bot python scripts/set_admin.py <telegram_user_id>
```

**Пример:**
```bash
docker exec -it gigabridge_bot python scripts/set_admin.py 123456789
```

> 💡 **Как узнать Telegram ID?** Отправьте боту `/start`, затем выполните команду выше с вашим ID. Если пользователя нет в базе, он будет создан автоматически.

### По username

```bash
docker exec -it gigabridge_bot python scripts/set_admin.py @username
```

**Пример:**
```bash
docker exec -it gigabridge_bot python scripts/set_admin.py @myusername
```

> ⚠️ **Важно:** При использовании username пользователь должен сначала отправить боту `/start`, чтобы его данные были в базе.

### Добавление нескольких администраторов

Просто выполните команду для каждого пользователя:

```bash
# Первый админ
docker exec -it gigabridge_bot python scripts/set_admin.py 123456789

# Второй админ
docker exec -it gigabridge_bot python scripts/set_admin.py 987654321

# Третий админ по username
docker exec -it gigabridge_bot python scripts/set_admin.py @admin_user
```

## Использование админ-панели

### Доступ к админ-панели

Администратор может открыть админ-панель командой:
```
/admin
```

### Добавление сервера

1. Откройте админ-панель (`/admin`)
2. Выберите "🖥️ Управление серверами"
3. Нажмите "➕ Добавить сервер"
4. Следуйте инструкциям бота:
   - Введите название сервера
   - Введите API URL
   - Введите API ключ
   - Введите цену
   - Введите локацию (или "-" чтобы пропустить)
   - Введите описание (или "-" чтобы пропустить)
   - Введите максимальное количество пользователей (или "-" чтобы пропустить)

### Редактирование сервера

1. Откройте "📋 Список серверов"
2. Выберите нужный сервер
3. Выберите параметр для изменения
4. Введите новое значение

### Удаление сервера

1. Откройте сервер для редактирования
2. Нажмите "🗑️ Удалить сервер"
3. Подтвердите удаление

## Безопасность

- Все команды админ-панели защищены проверкой прав доступа
- Только назначенные администраторы могут использовать админ-панель
- При попытке неавторизованного доступа команды не выполняются

